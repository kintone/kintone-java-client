package com.kintone.client.model.app.layout;

/** This enum represents the type of rows in form layouts. */
public enum LayoutType {

    /** A normal row of fields. */
    ROW,

    /** A Table. */
    SUBTABLE,

    /** A Group field. */
    GROUP
}
