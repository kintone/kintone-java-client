package com.kintone.client.model.app;

/** The type of source data that is to be copied by Actions of an App. */
public enum AppActionSourceType {

    /** Data stored in the source app's field */
    FIELD,
    /** The Record's permalink */
    RECORD_URL
}
