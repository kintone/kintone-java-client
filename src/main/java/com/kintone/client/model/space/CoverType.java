package com.kintone.client.model.space;

/** An enum for representing the cover image type of Spaces. */
public enum CoverType {

    /** The cover image is a preset image. */
    PRESET,

    /** The cover image is an uploaded image. */
    BLOB
}
